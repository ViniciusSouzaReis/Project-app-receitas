import React from 'react';
import customRender from './services/customRender';
import Recipes from '../pages/Recipes';

const pathname = '/meals';

describe('Teste da página de Recipes', () => {
  test('Se a página de recipes renderiza', () => {
    const providerProps = {
      value: '',
    };
    // customRender(<Recipes />, { providerProps }, pathname);
  });
});
